% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{adaptels_raster}
\alias{adaptels_raster}
\title{Segment a raster and write the labels}
\usage{
adaptels_raster(input, output = NULL, ..., connectivity = FALSE, min_size = 0)
}
\arguments{
\item{input}{A `SpatRaster`, a path, or several paths taken as bands.}

\item{output}{Path for the label raster. `NULL` writes nothing.}

\item{...}{Passed to [adaptels()].}

\item{connectivity}{Apply [enforce_connectivity()] to the result before
writing. Off by default, because it changes the adaptel count.}

\item{min_size}{Passed to [enforce_connectivity()] when `connectivity` is
`TRUE`.}
}
\value{
Invisibly, the list [adaptels()] returns.
}
\description{
Segment a raster and write the labels
}
\details{
Nodata is written as `-9999`, matching plGeoAdaptels, and set as the
raster's NA value so that terra and GDAL both read it back as missing
rather than as an adaptel numbered minus nine thousand.

Requires \pkg{terra}.
}
\examples{
\donttest{
if (requireNamespace("terra", quietly = TRUE)) {
  r <- terra::rast(nrows = 20, ncols = 24, nlyrs = 3,
                   vals = runif(1440, 0, 255))
  seg <- adaptels_raster(r, output = NULL, threshold = 60)
  seg$n_adaptels
}
}
}
